{
  "name": "cute-anger-meter",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "homepage": "https://adarshpatil0103.github.io/mood-pal/",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 47821",
    "build": "tsc && vite build",
    "build:pages": "npm run build && node scripts/sync-github-pages.mjs",
    "preview": "vite preview --host 127.0.0.1 --port 47821"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vite": "^8.2.2"
  }
}
